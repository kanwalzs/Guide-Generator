Usage: 

In cursor claude or claude code use the following prompt (as example): 

```
Using the prompt @new-template-generation.md can you generate a template using input <input file e.g. @data-quality-monitor.md>
```